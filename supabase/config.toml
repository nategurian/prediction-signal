# Supabase CLI — local dev + migration history (remote: use `supabase link`).
project_id = "prediction-signals"

[api]
enabled = true
port = 54321

[db]
port = 54322
major_version = 15

[studio]
enabled = true
port = 54323
